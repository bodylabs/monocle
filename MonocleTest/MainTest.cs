using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

using Monocle;

namespace MonocleTest
{
    [TestClass]
    public class MainTest
    {
        [TestMethod]
        public void AutomatedCaptureStartingTest()
        {
            /*
            MainWindow mainWindow = new MainWindow();
            PrivateObject privateMainWindow = new PrivateObject(mainWindow);
            privateMainWindow.Invoke("Button_Click", new object[] { null, null });
             */
        }
    }
}
